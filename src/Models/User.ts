export type UserProfileToken = {
  userName: string
  email: string
  token: string
  walletBalance: number 
}

export type UserProfile = {
  userName: string
  email: string
  walletBalance: number 
}